spring.application.name=url-shortening-service
server.port=${PORT:8082}

# DATABASE - Supabase Direct Connection
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false

# Supabase uses SSL — required for direct connections
spring.datasource.hikari.connection-init-sql=SET search_path TO public

# GATEWAY INTEGRATION
server.forward-headers-strategy=framework

# Base URL must match a valid gateway route that points to the redirect service
app.base-url=${APP_BASE_URL:http://localhost:8080/api/v1/redirect/}