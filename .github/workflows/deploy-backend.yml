name: Deploy Backend to GCP Cloud Run

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'url-shortening-service/**'

jobs:
  deploy-to-cloud-run:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: url-shortening-service
    steps:
      - uses: actions/checkout@v4
      
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_CREDENTIALS }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Authorize Docker push
        run: gcloud auth configure-docker us-central1-docker.pkg.dev

      - name: Build and Push Docker image
        run: |
          docker build -t us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/url-shortener-repo/backend:${{ github.sha }} .
          docker push us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/url-shortener-repo/backend:${{ github.sha }}

      - name: Deploy to Cloud Run
        uses: google-github-actions/deploy-cloudrun@v2
        with:
          service: url-shortener-backend
          image: us-central1-docker.pkg.dev/${{ secrets.GCP_PROJECT_ID }}/url-shortener-repo/backend:${{ github.sha }}
          region: us-central1
          env_vars: |
            DB_URL=${{ secrets.DB_URL }}
            DB_USERNAME=${{ secrets.DB_USERNAME }}
            DB_PASSWORD=${{ secrets.DB_PASSWORD }}
            APP_BASE_URL=${{ secrets.APP_BASE_URL }}
